char* int2binary(int num, int bits);

int char2int (char *word);

void printArray(char **arr, int len);
void newMachine(char *line);
void change_file_extension(char* filename, char* new_ext);
void write_files(HashTable *Symbol_Table, char **Iarr, char **Darr, int IC, int DC, char *file_name);